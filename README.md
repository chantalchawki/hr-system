# HR system

To run the backend:

```bash
cd backend
docker-compose up -d
```

To run the frontend:

```bash
cd frontend
npm start
```

Postman collection:
[postman-collection](./documentation/hr-system.postman_collection.json)